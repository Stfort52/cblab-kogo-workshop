name: scRNA-seq trajectory analysis

on:
  push:
    branches: ["main"]

jobs:
  test:
    runs-on: self-hosted

    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Build
      run: docker build -t kogo-scrna-jupyter client

    - name: Run notebook
      run: |
        timeout 1800 docker run --rm \
          --memory="8G" \
          --cpus=2.4 \
          -v ${{ github.workspace }}/notebooks:/workspace \
          -v ${{ env.HOST_DATA_DIR }}:/workspace/data:ro \
          -w /workspace \
          kogo-scrna-jupyter:latest \
          jupyter nbconvert \
            --inplace \
            --execute \
            monocle.ipynb
    
    - name: Upload failures
      if: failure()
      uses: actions/upload-artifact@v4
      with: 
        name: failed-notebooks
        path: ${{ github.workspace }}/notebooks/*.ipynb
        if-no-files-found: warn
    
    - name: Cleanup Image
      if: always()
      run: docker rmi scrna-jupyter:latest || true

